[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
addopts =
    --tb=short
    -ra
    --cov=app
    --cov-report=term-missing 
    --cov-report=html:tests/htmlcov
    --cov-config=.coveragerc
    --cov-fail-under=85